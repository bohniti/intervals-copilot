// Allow importing CSS files (e.g. leaflet/dist/leaflet.css) as ES modules.
// TypeScript has no built-in type for CSS module side-effect imports, so we
// declare a wildcard for them here.
declare module "*.css";
